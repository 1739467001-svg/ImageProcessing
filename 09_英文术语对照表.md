# 📖 中英术语对照表 · Glossary (English ↔ 中文)

> 给陈俊烨 · 英方(Sussex)英文考试专用。**全部英文取自课程讲义/官方答案的实际写法 + 英式拼写**(由agent逐章提取)。
> 用法：考前扫一遍；写英文答案时"中文概念→直接套英文"。备注列标了英式拼写陷阱和官方常用短语。
> **共 650 条术语**，分 9 个主题。

## ⚠️ 英式拼写铁律(Sussex卷必用)
`colour`(非color) · `grey`(非gray) · `centre`(非center) · `neighbour`(非neighbor) · `normalise/quantise/minimise`(-ise非-ize) · `labelling/modelling`(双l) · `behaviour` · `analyse` · `analogue`(非analog)

---

- 1. Optics & Imaging 光学与成像
- 2. 图像基础：采样/量化/插值/几何变换 Fundamentals
- 3. 图像增强:点变换/直方图 Enhancement
- 4. Segmentation & Colour (分割与颜色)
- 5. 滤波/形态学/噪声 Filtering, Morphology & Noise
- 6. 卷积与边缘检测 Convolution & Edges
- 7. 描述子:矩 / Hough变换 / 傅里叶描述子 (Descriptors: Moments / Hough Transform / Fourier Descriptors) — Week 6
- 8. 分类与深度学习:CNN/池化/BatchNorm (Classification & Deep Learning)
- 9. 互相关 / SIFT / 图像配准RANSAC / Viola-Jones 术语对照表（Week 8 + Week 9，英式拼写）

---

## Optics & Imaging 光学与成像

| 中文 | English | 备注 |
|---|---|---|
| 电磁波谱 | **electromagnetic spectrum** | 光是其中人眼可感知的部分;考点:按 wavelength 波长划分 |
| 波长 | **wavelength** | 符号 λ;公式题用 λ=500nm;短波蓝光分辨率更高 |
| (反射)光 / 反射 | **reflected light / reflect** | 答题:'wavelengths are absorbed, the rest is reflected';感知颜色由反射光决定 |
| 颜色 / 色彩 | **colour** | 英式拼写!不是 color;课件标题如 Colour Cameras / Colour Image Processing |
| 照明 / 照亮场景 | **illumination / illuminating a scene** | 成像两要素之一;答题短语 'illuminating a scene and absorbing the reflected energy' |
| 光学 / 几何光学 | **optics / geometric optics** | ray diagram 光线图;该课节标题就叫 'Optics' |
| 斯涅尔定律(折射定律) | **Snell's Law** | n = sinΘ1 / sinΘ2;水中实验题必引用它解释 power of the lens 变化 |
| 折射 / 弯折 | **refract (aka bend)** | 光进入高折射率介质会 bent towards the normal 向法线弯折 |
| 折射率 | **refractive index (refraction factor n)** | 易错:water 的折射率 > air,会使系统 out of focus |
| 法线 | **normal (of the surface)** | 折射方向描述:'bent towards the normal' |
| 凸透镜 / 凹(负)透镜 | **convex lens / negative lens** | convex 汇聚 converge;negative lens causes rays to diverge 发散 |
| 焦点 | **focal point** | 平行入射光汇聚的点;converge to one point |
| 焦距 | **focal length, f** | a measure of the power of the lens;f 越小透镜越 powerful |
| 光焦度(屈光度) | **power (diopter)** | 课件/答案拼作 'diaopter'(原文笔误);两透镜相贴 total power = D1+D2,f=1/D |
| 光线 / 边缘光线 | **ray / extreme rays** | 光源 emits rays in every direction,光学系统只取 extreme rays |
| 薄透镜 / 厚透镜 | **thin lens / thick lens** | ray diagram 简化模型 |
| 针孔相机模型 | **pinhole camera model** | 标题 'Simplification of Imaging – Pinhole Camera Model' |
| 成像公式 / 透镜成像公式 | **lens (maker's) formula** | 课件写 'Lens Markers Formula';1/f = 1/u + 1/v |
| 放大率 | **magnification, m** | 'systems magnification';用相似三角形 ratio of triangles 求 |
| 物距 / 像距 | **object distance u / image distance v** | u,v are distances to true focal planes;像在镜后用 v |
| 光轴 | **optical axis** | 物/像沿 optical axis 移动定义景深 |
| 光圈数 / F数 | **f-number, N** | N = f/D (= f/N,N 为 light-in diameter);越大进光越少 less light reaches sensor |
| (透镜)孔径 / 直径 | **aperture / lens diameter D** | max light-in diameter;f-number 由它和 f 决定 |
| 景深 | **depth of field** | 物面沿光轴可移动且仍清晰的范围;考点区分 depth of focus |
| 焦深 | **depth of focus** | 像面(如 CCD position)可移动仍清晰的范围;取决于 lens 和 pixel size |
| 对焦 / 失焦 | **in focus / out of focus** | 答题:object shifted past the focal plane → forms image behind camera, out of focus |
| 焦平面 | **focal plane** | object shifted past/before the focal plane |
| 弥散圆 / 混淆圆 | **circle of confusion, C** | = pixel size;all rays must land within C for the object to be in focus |
| 超焦距 / 超焦平面 | **hyperfocal plane / hyperfocal length, H** | 课件笔误 'hyper focal plan';H = f²/(N·C);Beyond H all objects are in focus |
| 近/远景深距离 | **near and far depth-of-field distances (S, R)** | S = Hu/(H−u), R = Hu/(H+u);total depth of field = S − R |
| 像差 | **aberration** | f-number controls depth of focus, resolution and some of the aberrations |
| 分辨(解析) | **resolve / cannot resolve the blur** | 答题短语:'if the blur is smaller than a pixel, the pixel cannot resolve the blur' |
| 光学分辨率 / 数值孔径 | **optical resolution / numerical aperture (NA)** | r = 1.22λ/(2·NA);NA ≈ 1/(2N);需两个像素才能 resolve(r = 2 pixels) |
| 视场 / 视场角 | **field of view (FOV)** | 题:object fills 90% of field of view |
| 图像采集 / 获取 | **image acquisition** | process chain 第一步;= capturing a digital image of a real-world scene |
| 成像传感器 / 传感器阵列 | **imaging sensor / array of image sensors** | 也有 line of image sensors、sensor strips and rings;incoming energy lands on sensor material |
| 电荷耦合器件 | **Charge-Coupled Device (CCD)** | 全称 Charged Couple Devices;本质 a series of MOS capacitors |
| 互补金属氧化物半导体(传感器) | **CMOS (sensor)** | array of photo-diodes;分 passive / active (APS) pixel sensor |
| 光子 / 入射量子 | **photon / incident quanta** | Photon interaction causes electron-hole pairs;E = hν |
| 电子-空穴对 | **electron-hole pair** | 光子在 silicon 中产生;dark noise 来自随机 electron-hole pairs |
| 耗尽层 | **depletion layer** | 正电压加在 electrode 下形成 a depletion layer of free electrons |
| 电极 / 栅极 | **electrode (gate)** | three electrodes (or gates) per pixel;transparent electrode on top |
| 电荷转移效率 | **Charge Transfer Efficiency (CTE)** | S-CCD ~99.99%;buried channel CCD (B-CCD) 更高 |
| 三 CCD / 分光棱镜方案 | **three sensor solution / trichroic beam splitter** | splits light to three separate sensors;好结果但昂贵 |
| 拜耳滤色阵列 | **Bayer filter** | RGBG layout;绿多因 human eye more sensitive to green;缺点 interpolated image + colour aliasing |
| 色彩混叠 | **colour aliasing** | 英式 colour;RGB very sensitive to periodic objects → colour aliasing |
| 光谱响应 | **spectral response** | 课件节标题 |
| 量子效率 | **quantum efficiency, Q** | photoelectrons per second per incident quanta;Power onto CCD = nEQ/t |
| 光电子 | **photoelectron** | quantum efficiency 定义中:number of photoelectrons per second |
| 填充因子 | **fill factor** | CMOS has a lower fill factor than CCD |
| 全局电子快门 | **global electronic shutter** | CCD 有;CMOS 实现起来困难得多 |
| 噪声(多种) | **noise: fixed pattern / shot / dark / read-out noise** | shot noise 服从 Poisson distribution;cooling the sensor reduces dark noise |
| 动态范围 | **dynamic range** | CCD 约 2–3 decades;larger bit depth → larger dynamic range |
| 饱和 | **saturation / saturate** | Saturation is the highest...;bright regions do not saturate the CCD as easily |
| 位深 / 模数转换器 | **bit depth / ADC (analogue-to-digital conversion)** | 英式 analogue 不是 analog;most common 8 bit (256 levels) |
| 采样 | **sampling** | 采样因子 M and N;digital sensor measures a limited number of samples at a discrete set of points |
| 量化 | **quantisation** | 英式拼写!不是 quantization;converting a continuous value to a discrete one |
| 空间分辨率 | **spatial resolution** | 课件高频术语;refers to the smallest discernible detail / number of pixels (M×N) |
| 灰度级分辨率(位深) | **intensity (level) resolution / grey levels** | 英式 grey 不是 gray;256 grey levels = 8 bits per pixel (bpp) |
| 像素 / 像素间距 | **pixel / pixel pitch** | 题中 pixel pitch = 10µm;circle of confusion = pixel size |
| 视网膜 / 视锥-视杆细胞 | **retina / cones and rods** | lens focuses light onto the retina;cones sensitive to colour, rods to low illumination |

## 图像基础：采样/量化/插值/几何变换 Fundamentals

| 中文 | English | 备注 |
|---|---|---|
| 采样（取样） | **sampling** | 对空间坐标的离散化；'how sampling was carried out' 决定 spatial resolution |
| 量化 | **quantisation** | 英式拼写必须用 -s-，不要写 quantization；定义：converting a continuous analogue signal into a digital representation |
| 模拟信号（连续） | **analogue signal** | 英式拼写 analogue，不要写 analog；与 digital 相对 |
| 插值 | **interpolation** | 几何变换两步之一：intensity interpolation 强度插值 |
| 几何变换 | **geometric transformation** | = spatial transformation of coordinates + intensity interpolation |
| 空间变换（坐标变换） | **spatial transformation** | 课件原话 'a spatial transformation of coordinates'；transformation of coordinates 写成 (x,y)=T{(v,w)} |
| 采样因子 | **sampling factor** | 课件 'sampling factors M and N should be positive integers'，常取 2^i |
| 分辨率 | **resolution** | 问 'how much is enough?' |
| 空间分辨率 | **spatial resolution** | determined by how sampling was carried out；指 smallest discernable detail |
| 强度级分辨率（位深） | **intensity level resolution / bit depth** | = number of intensity levels；课件标题 'Intensity Level Resolution ---- Bit Depth' |
| 灰度级 | **grey level** | 英式拼写 grey 不要写 gray；范围 0-255 (black-white)。注意：图片标注里出现 'grey levels' |
| 强度（灰度值） | **intensity** | perceived intensity 感知强度；actual intensity 实际强度；intensity level 强度级 |
| 像素 | **pixel** | pixel size 像素大小；pixel value 像素值 |
| 每英寸点数 | **dots per inch (DPI)** | 图形设计常用；vision specialists 谈 pixel size |
| 图像表示 | **image representation** | digital image composed of M rows and N columns of pixels |
| 行 / 列 | **row / column** | M rows and N columns；f(row, col) |
| 近似 | **approximation** | 考点原话：'a digital image is always only an approximation of a real world scene' |
| 动态范围 | **dynamic range** | the ratio of the maximum measurable intensity to the minimum detectable intensity；upper limit by saturation, lower limit by noise |
| 饱和 | **saturation** | the highest value beyond which all intensity levels are clipped（被裁剪 clipped） |
| 噪声 | **noise** | 决定 dynamic range 的下限 lower limit；denoise 去噪：assume the mean value of noise is 0 |
| 图像对比度 | **image contrast** | the difference in intensity between the highest and lowest intensity levels；low contrast 看起来 dull, washed-out |
| 亮度适应 | **brightness adaptation** | 注意 brightness 不要拼成 brightless |
| 同时对比（同时对比效应） | **simultaneous contrast** | 课件斜体专有叫法 |
| 马赫带 | **Mach bands** | 感知现象，B 大写 |
| 像素邻域 | **neighbours of a pixel** | 英式拼写 neighbour（注意题干图片用美式 neighbors，但答题请用 neighbour） |
| 4-邻域 | **4-neighbours** | horizontal and vertical neighbours，记作 N4(p) |
| 对角邻域 | **D-neighbours** | the 4 diagonal neighbours，记作 ND(p) |
| 8-邻域 | **8-neighbours** | 4-neighbours + D-neighbours combined，记作 N8(p) |
| 邻接性 | **adjacency** | 分 4-adjacency / 8-adjacency / m-adjacency；V 是定义邻接用的 intensity values 集合 |
| m-邻接（混合邻接） | **m-adjacency** | a modification of 8-adjacency, introduced to eliminate the ambiguities that arise when 8-adjacency is used |
| 连通性 | **connectivity** | connected 连通；connected component 连通分量；connected set 连通集 |
| 通路（路径）/ 闭合通路 | **path (curve) / closed path** | length of the path 路径长度；若起点=终点则为 closed path |
| 区域 / 相邻 / 不相交 | **region / adjacent / disjoint** | region = a connected set；不相邻的区域 said to be disjoint |
| 前景 / 背景 | **foreground / background** | 背景邻居 background neighbour 用于判定 boundary |
| 边界（内边界/轮廓） | **boundary (inner border / contour)** | the set of pixels in the region that have at least one background neighbour；is a 'global' concept |
| 外边界 | **outer border** | 用于 border-following algorithms，保证结果是 closed path |
| 边缘 | **edge** | a 'local' concept；formed from pixels with derivative values that exceed a preset threshold；edges = intensity discontinuities |
| 距离度量 / 度量 | **distance measure / metric** | distance function or metric，满足非负、对称、三角不等式 |
| 欧氏距离 | **Euclidean distance (De)** | sqrt((x-s)^2+(y-t)^2) |
| 城市街区距离 | **city-block distance (D4)** | \|x-s\|+\|y-t\|；又称曼哈顿距离 |
| 棋盘距离 | **chessboard distance (D8)** | max(\|x-s\|,\|y-t\|)；chessboard 一个词 |
| 线性算子 / 非线性 | **linear operator / nonlinear** | H is a linear operator if H[a_i f_i + a_j f_j] = a_i H[f_i]+a_j H[f_j]；max operation is not a linear operator |
| 算子（一般算子） | **operator (general operator H)** | H[f(x,y)]=g(x,y)，input image f → output image g |
| 阵列积 vs 矩阵积 | **array product vs matrix product** | array product = 逐元素相乘 element-wise；matrix product = 矩阵乘法 |
| 算术（阵列）运算 | **arithmetic (array) operations** | 加 s=f+g, 减 d=f-g, 乘 p=f×g, 除 v=f÷g；differencing 求差用于 obtain blood vessels |
| 集合运算 / 逻辑运算 | **set operations / logical operations** | 并集 union (A∪B)、交集 intersection (A∩B)、补集 complement (A^c)、差 A−B；逻辑 NOT/AND/OR/XOR/AND-NOT |
| 单像素运算 | **single pixel operations** | s=T(z)，z 是原图强度，s 是处理后强度；T(z) 也叫 intensity transformation function |
| 邻域运算 | **neighbourhood operations** | 如 local averaging 局部平均：g(x,y)=(1/mn)Σ f(r,c)；Sxy 是以 (x,y) 为中心的邻域坐标集 |
| 几何空间变换与图像配准 | **geometric spatial transformations and image registration** | 章节标题；need to model the camera |
| 针孔相机模型 | **pinhole camera model** | 3D→2D；all rays pass through a pinhole = centre of the camera；x=fX/Z, y=fY/Z（注意 centre 英式拼写） |
| 齐次坐标 | **homogeneous coordinates** | (x,y,w) ⇔ Cartesian (x/w, y/w)；N+1 维表示 N 维；具有规模不变性 scale invariance |
| 仿射变换 | **affine transform / affine transformation** | a 2D-2D linear transformation；keeps straightness（直线性）and parallelness（平行性） |
| 刚体变换 | **rigid transform** | 只含旋转+平移；与 affine、projective 对比 |
| 投影变换 | **projective transform / project transform** | 课件写作 'project transform'；梯形畸变那种 |
| 恒等变换 | **identity (transform)** | affine matrix 表里第一行 x=v, y=w |
| 缩放（比例变换） | **scaling** | x=cx·v, y=cy·w；图像缩放因子 scale factor |
| 旋转 | **rotation** | x=v cosθ − w sinθ, y=v cosθ + w sinθ；rotated 21° 是经典插值例子 |
| 平移 | **translation** | x=v+tx, y=w+ty；homography 里的 shift (4,5) |
| 错切（剪切） | **shear** | 分 shear (vertical) 和 shear (horizontal)；skew 0.1 即剪切量 |
| 放大倍率 | **magnification** | quiz 原话 'a magnification of 2.5 in the x direction, and 2.6 in the y'；M=−y'/y |
| 单应（性）矩阵 | **homography matrix** | quiz 解答用 H（3×3），含 magnification/shift/skew；convert to homographic coords by setting z=1 |
| 重采样 | **resampling** | x',y' are the new resampled coords；改变 sample rate |
| 最近邻插值 | **nearest neighbour interpolation** | I'(x',y')=I(round(x'),round(y'))；fast but gives a ragged edge（锯齿边） |
| 双线性插值 | **bilinear interpolation** | 先沿 x 方向线性插值，再沿 y 方向；quiz 必考。注意：先 in the x-direction 再 y-direction |
| 双三次插值 | **bicubic interpolation** | g(x',y')=ΣWx·Wy·g(·)，用权重 Wx,Wy；比 bilinear 边缘更平滑 |
| 像素间距 | **pixel spacing** | quiz 解法：new pixel spacing = old size / new size，如 6/4.8=1.25 pixels |
| 直方图 | **histogram** | histogram reveals nothing about the spatial information；poor contrast 体现在直方图集中 |
| 对比度拉伸（最大化对比度） | **maximise the contrast** | quiz1 答题动词，英式 maximise；公式 f(I)=(I−Min)/(Max−Min)*255 |
| 归一化（区间归一化） | **normalise the range** | 英式 normalise 不要写 normalize；subtract off the min value 再 normalise |
| 焦距 | **focal length (f)** | Lens Makers Formula: 1/f = 1/u + 1/v（课件误拼 'Lens Markers'） |
| 光圈数 / F 数 | **F-number (N = f/D)** | the higher the f-number, the less light reaches the sensor |
| 景深 / 焦深 | **depth of field / depth of focus** | depth of field 物方范围 (object remains in focus)；depth of focus 像方/CCD 范围。易混 |
| 弥散圆（模糊圈） | **circle of confusion (C)** | = pixel size；all rays must land within C for the object to be in focus |
| 超焦距 | **hyperfocal plane** | 课件写作 'hyper focal plan'；beyond H all objects are in focus |
| 电磁波谱 | **electromagnetic spectrum** | light is just a particular part of it；split up according to wavelengths |
| 波长 | **wavelength** | visible spectrum 约 400–700 nm |
| 反射光 / 吸收 | **reflected light / absorbed** | colours we perceive determined by the nature of the light reflected；most wavelengths are absorbed（注意 colour 英式拼写） |
| 图像传感 / 采集 | **image sensing / acquisition** | incoming energy lands on a sensor material → generates a voltage；用 sensor strips and rings |
| 等偏爱曲线 | **isopreference curves** | 在 Nk-plane 内研究 N 与 k 的关系；曲线上的点主观质量相等 |

## 图像增强:点变换/直方图 Enhancement

| 中文 | English | 备注 |
|---|---|---|
| 图像增强 | **image enhancement** | 全章主题;定义=making images more useful |
| 点处理 / 点运算 | **point processing** | 邻域=像素本身;同义 point processing operation |
| 点强度变换 | **point intensity transforms** | 讲义分区标题(POINT INTENSITY TRANSFORMS) |
| 灰度级变换函数 | **grey level transformation function** | 英式grey;= point processing operation;讲义记 s = T(r) |
| 强度变换 | **intensity transformation** | 幻灯片标题 Intensity Transformations |
| 空间域(技术/方法) | **spatial domain (techniques)** | 对像素直接操作 direct manipulation of image pixels |
| 频率域(技术/方法) | **frequency domain (techniques)** | 操作 Fourier transform / wavelet transform |
| 算子 | **operator** | g(x,y)=T[f(x,y)],T是定义在邻域上的operator |
| 邻域 | **neighbourhood** | 英式拼写neighbourhood(美式neighborhood);考试务必双写法注意 |
| 输入图像 / 处理后图像 | **input image / processed image** | f(x,y)输入, g(x,y)处理后(processed/enhanced image) |
| 原始像素值 / 处理后像素值 | **original image pixel value / processed image pixel value** | s = T(r):r原始,s处理后 |
| 负片图像 / 取反 | **negative images** | 公式 s = 1.0 − r 或 s = intensity_max − r;用于dark regions里的white/grey detail |
| 嵌入在暗区中的细节 | **detail embedded in dark regions** | 答题短语:enhancing white or grey detail embedded in dark regions |
| 阈值化 / 二值化 | **thresholding** | thresholding transformation;s=1.0 if r>threshold else 0.0 |
| 分割 | **segmentation** | thresholding/histogram对segmentation特别有用;isolate an object of interest from a background |
| 对数变换 | **logarithmic transformation** | s = c * log(1 + r);通常 c=1;别写成log transform之外的形式 |
| 反对数变换 | **inverse log transformation** | performs the opposite transformation |
| 映射(窄范围→宽范围) | **maps a narrow range into a wider range** | log答题核心句:maps a narrow range of low input grey level values into a wider range of output values |
| 幂律(伽马)变换 | **power law (gamma) transformations** | s = c * r^γ;c通常=1;grey levels在[0.0,1.0] |
| 伽马 γ | **gamma (γ)** | varying γ gives a whole family of curves |
| 伽马校正 | **gamma correction** | 显示器gamma correction;display devices do not respond linearly;s=r^(1/2.5)=r^0.4 |
| 亮度(显示) | **luminance** | gamma correction图:I ~ U^γ, γ=2.0…2.3 |
| 对比度拉伸 | **contrast stretching** | g = (I−I1)/(I2−I1)·(I_max−I_min)+I_min;改善poor contrast |
| 对比度规约/指定 | **contrast specification** | 讲义:applying a pretty simple contrast specification |
| 感兴趣强度范围 | **intensity range of interest** | I1, I2;I_max/I_min = max and min possible display value |
| 分段线性变换函数 | **piecewise linear transformation functions** | arbitrary user-defined transforms;含contrast stretching linear transform |
| 灰度级切片 | **grey level slicing** | 标题写Gray Level Slicing(美式),正文grey;highlights a specific range of grey levels |
| 抑制 / 保留(其他灰度级) | **suppressed or maintained** | grey level slicing答题:other levels can be suppressed or maintained;区间[A,B] |
| 位平面切片 | **bit plane slicing** | isolating particular bits;每个bit plane是binary image |
| 高位 / 低位比特 | **higher-order bits / lower-order bits** | higher-order=most significant visual info;lower-order=subtle details |
| 最高有效位 / 最低有效位 | **most significant bit / least significant bit** | Bit-plane 7 (most significant), Bit-plane 0 (least significant);8-bit byte |
| 图像直方图 | **image histogram** | shows the distribution of grey levels in the image |
| 灰度级分布 | **distribution of grey levels** | 直方图定义短语 |
| 频数(频率) | **frequency / frequencies** | 直方图纵轴Frequencies;equalisation里 n_j=frequency of intensity j, n=sum of all frequencies |
| 暗 / 亮 / 低对比度 / 高对比度图像 | **dark / bright / low-contrast / high-contrast image** | histogram examples四类;high contrast image has the most evenly spaced histogram |
| 直方图均衡化 | **histogram equalisation** | 英式-isation(讲义标题处也出现美式equalization,英方考试用equalisation);spreading out the frequencies |
| 均衡化变换函数 | **equalisation transformation function** | s_k = T(r_k) = Σ p_r(r_j) = Σ n_j/n |
| 累积分布函数 | **cumulative distribution function (CDF)** | 均衡化曲线即CDF;考试常考equalisation映射=CDF |
| 输入/处理后强度 | **input intensity / processed intensity** | r_k输入, s_k处理后, k=intensity range (e.g. 0.0–1.0) |
| 全局 / 局部直方图均衡化 | **global / local histogram equalisation** | local用neighbourhood(如3×3);Fig 3.26 |
| 对比度受限直方图均衡化 | **contrast limited histogram equalisation (CLHE)** | 通过clipping histogram values限制对比扩张 |
| 对比度受限自适应直方图均衡化 | **contrast limited adaptive histogram equalisation (CLAHE)** | 基于图像局部直方图;Pizer, Amburn et al. 1987 |
| 滑动窗口法 / 分块(瓦片)法 | **sliding window approach / tiling approach** | CLAHE两种;tiling=subdivide into overlapping regions, smooth blending between neighbouring tiles |
| 裁剪直方图值 | **clipping histogram values** | limit contrast expansion in flat regions by clipping;裁掉超过H_max的部分 |
| 双线性插值 | **bilinear interpolation** | CLAHE末步:对四个直方图映射函数做双线性插值(讲义中文'双线性插值') |
| 平坦/平滑、噪声区域 | **flat regions / smooth, noisy regions** | CLAHE在flat regions限制扩张;均衡化缺点=放大smooth noisy regions的noise |
| 放大噪声 | **enhancement of the noise** | equalisation缺点答题:shows significant enhancement of the noise |

## Segmentation & Colour (分割与颜色)

| 中文 | English | 备注 |
|---|---|---|
| 分割 | **segmentation** | 本课核心标题词;'segment our objects of interest'(动词) |
| 二值化 | **binarisation** | 英式拼写 -isation 不是 -ization;讲义 'This Week' 列表用词 |
| 二值图像 | **binary image** | 公式注释:B_{i,j} – the resultant binary image |
| 阈值 | **threshold** | T -threshold;动词 'to threshold an image' 也常用 |
| 阈值化 | **thresholding** | 'histogram thresholding','double thresholding';双写无,单 l 结尾 |
| 类 | **class** | 'a group of objects with a common property' |
| 类内 / 同类 | **in-class** | 易考:'In-class intensity variance';refers to pixels within the same class |
| 类间 / 异类 | **out-of-class** | 与 in-class 对应;pixels that are in different classes |
| 前景 | **foreground** | 'White is foreground';答题常说阈值升高 shrinks the foreground |
| 背景 | **background** | background class;背景图记作 I_bk(x,y) |
| 灰度 / 灰阶 | **grey level / grey value** | 英式 grey 不是 gray!'one grey level at a time','histogram of grey values' |
| 灰度图 | **greyscale** | 英式 greyscale;'convert to greyscale',I_grey(x,y) |
| 强度 / 亮度值 | **intensity** | 'mean intensity','intensity variance';轴标 Intensity |
| 直方图 | **histogram** | 'histogram of grey values';discards spatial information |
| 直方图峰 | **histogram peaks** | 'Noise tends to broaden out the histogram peaks';peaks widely separated |
| P 分位法 / P-瓦片法 | **P-tiles (the P-Tiles method)** | 课特有叫法;'object occupies P% of an image area';'This simple method has quite limited use' |
| 迭代阈值选择 | **iterative threshold selection** | quiz 主考点;又称 iterative (grey-level) thresholding algorithm |
| 初始阈值猜测 | **initial threshold guess** | 'the mean intensity is a good start';记作 T |
| 均值 / 平均 | **mean** | 'same as calculating the centre of mass';新阈值 T=½(m1+m2) |
| 质心 | **centre of mass** | 英式 centre 不是 center;mean = centre of mass of histogram |
| Otsu 方法 | **Otsu's method** | 讲义拼 Otsu,quiz 手误拼 Ostu;'maximum sigma_between'(类间方差) |
| 类间方差 | **between-class variance (sigma_between)** | Otsu 最大化目标;代码变量 sigmaBt |
| 自适应阈值 | **adaptive thresholds** | 讲义 'This Week' 列表;用于光照不均 |
| 光照不均 / 不均匀照明 | **uneven / non-uniform illumination** | 'Neither method work if the illumination is uneven';lighting not even |
| 双阈值 | **double threshold / double thresholding** | 用 T1(低)和 T2(高);watershed 是它的发展 |
| 移动阈值 | **moving threshold** | 双阈值图示标注;从 T1 升到 T2 |
| 四叉树分解 | **quadtree decomposition** | 'splitting the image into smaller and smaller regions' |
| 象限 | **quadrant** | 'divide into 4 quadrants, R1,R2,R3,R4' |
| 谓词 / 判定函数 | **predicate** | 'Each region is tested by a predicate' Q(R);返回 TRUE/FALSE |
| 分裂与合并 | **split and merge** | split into quadrants if Q FALSE;'Merge any adjoining region where combined Q is TRUE' |
| 熵 | **entropy** | 可用于测试区域:intensity, colour, variance, entropy |
| 分水岭算法 | **watershed algorithm** | 'allows touching objects to be segmented';是 double thresholding 的发展 |
| 相接/接触的物体 | **touching objects** | 考点:'Touching objects cannot be segmented'(旧法局限);watershed 可分 |
| 边界扩张 / 物体边界 | **object boundaries expand** | 阈值升高 'object boundaries will expand';触碰时 not allowed to merge |
| 活动轮廓(蛇) | **active contours (snakes)** | 课特有别名 Snakes;讲义 'Other methods' 列表 |
| 运动分割 | **motion segmentation** | 用于视频;'object is moving and the background is stationary' |
| 背景减除 | **background subtraction** | \|I_bk(x,y) - I(x,y)\| > A;A~30 for 8bit camera |
| 背景更新参数 | **update parameter (α)** | 'between 0 and 1','larger α, the faster the update';I_bk_new=(1-α)I_bk_old+αI_new |
| 帧平均 | **average over N frames** | I_bk(x,y)=(1/N)ΣI_n(x,y);fast moving objects are washed out |
| 颜色 | **colour** | 英式 colour 全篇,绝不可写 color!标题 COLOUR SEGMENTATION |
| 颜色分割 | **colour segmentation** | 'detection of coloured objects';结论页 |
| 通道 | **channel** | '8 bits per channel';答题 'set individual thresholds for all 3 channels','sum across channels'(R+G+B) |
| 颜色空间 | **colour space** | 'change to a different colour-space';RGB/HSV/Lab/YUV |
| 加色模型 | **additive colour model** | RGB 'This is an additive colour model' |
| 减色模型 | **subtractive colour space** | CMYK 'is a subtractive colour space used in printing' |
| 绝对颜色空间 | **absolute colour spaces** | sRGB, Adobe RGB are unambiguous;useful for printing |
| 色度 (RG) | **RG chromaticity** | r=R/(R+G+B) 等;'Divide RGB by the intensity to remove it' |
| 色相 | **hue (H)** | 'Hue is the colour index';答题 Hue>0.11 selects yellow |
| 饱和度 | **saturation (S)** | 'degree of whiteness in the colour';'select on saturation removes white/dark regions' |
| 明度 / 值 | **value (V)** | HSV 的 V;'a measure of the intensity of the colour';分割时常 'Ignore V' |
| HSV / HSI 颜色空间 | **HSV (Hue, Saturation, Value)** | 'corresponds better to how people experience colour';公式页标 HSI |
| Lab 颜色空间 | **Lab colour space** | 'L is lightness','a and b are the colour components';'more perceptually uniform than HSV' |
| 感知均匀 | **perceptually uniform** | Lab 特性;'any change in values should produce a uniform change in colour' |
| 亮度 (Lab) | **lightness (L)** | 'L is a measure of the lightness (brightness)' |
| YUV 颜色空间 | **YUV** | 'used in analogue PAL TV';英式 analogue 不是 analog;Y=intensity, U/V 是两个 chrominance/colour channels |
| 亮度信号 (Luma) | **luma (Y)** | 'Y – Luma (analogous to brightness)' |
| 色度信号 | **chrominance (U, V)** | 'UV are two chrominance values' |
| YCrCb 颜色空间 | **YCrCb (Y, Cr, Cb)** | quiz 第2/3题用;由 RGB camera 转换而来;Y/Cr/Cb |
| CMYK 颜色空间 | **CMYK (Cyan, Magenta, Yellow, Black)** | 'subtractive colour space used in printing';K=min(C,M,Y) |
| 量化 | **quantisation / quantise** | 英式 -isation;quiz:'quantisation level q=256/level','quantised values of YCrCb' |
| 量化误差 | **(quantisation) error** | quiz:'What is the error on the RGB pixel...';'Error is 4, 6, 28' |
| 向下取整 / 取底 | **round down (floor ⌊·⌋)** | quiz 注:'⌊·⌋ means round down';区别 round() |
| 归一化 | **normalise** | 英式 -ise;'normalise histogram so sum equals 1'(Otsu 代码注释) |
| 零交叉 | **zero-crossing** | 边缘/二阶导常考短语;答题描述边缘位置用语 |
| 噪声 | **noise** | 'broaden out the histogram peaks';camera noise → use threshold A |
| 空间信息 | **spatial information** | 局限:'The histogram discards spatial information about the image' |
| 出现概率 | **probability of occurrence** | 'Most methods use size and probability of occurrence' |
| 伪代码 | **pseudo code** | quiz Q4:'Write a pseudo code algorithm';human readable not machine readable |

## 滤波/形态学/噪声 Filtering, Morphology & Noise

| 中文 | English | 备注 |
|---|---|---|
| 形态学算子 | **morphological operators** | 该课标题用法;也写 Morphological Operators |
| 结构元 | **structuring element** | 考试高频;讲义也写 structure element / SE;切勿写成 structural element(quiz Q4题干误用,但讲义标准叫法是 structuring element) |
| 二值掩膜/二值图像 | **binary mask / binary image** | 形态学操作对象;mask 即分割得到的二值图 |
| 邻域 | **neighbourhood** | 英式拼写,必带 u(不是 neighborhood);结构元中值为1的像素定义邻域 |
| 相邻像素 | **neighbouring pixels / adjoining pixels** | 英式 neighbouring;讲义两种说法都用 |
| 连通性 | **connectivity** |  |
| 4-连通 | **4-connected** | 边相接才算同一物体(水平/垂直方向);答题:'edges touch' |
| 8-连通 | **8-connected** | 边或角相接(水平/垂直/对角);答题:'edges or corners touch' |
| 连通分量 | **connected components** | quiz Q5:'4 level and 8 level connected components';数物体个数用 |
| 膨胀 | **dilation** | 符号 A ⊕ B;输出取邻域最大值;二值图中任一像素为1则输出1;'enlarge the objects','filled the holes' |
| 腐蚀 | **erosion** | 符号 A ⊖ B;输出取邻域最小值;二值图中任一像素为0则输出0;'erodes the edges','shrinks the foreground'(removes outside noise) |
| 开运算 | **opening** | A ∘ B = (A ⊖ B) ⊕ B;先腐蚀后膨胀;'smooth the contours, removes thin protrusions';去小物体/去噪点 |
| 闭运算 | **closing** | A • B = (A ⊕ B) ⊖ B;先膨胀后腐蚀;'fill narrow breaks and small holes' |
| 边界提取 | **boundary extraction** | β(A) = A − (A ⊖ B);腐蚀后从原图相减;边界粗细取决于 B 的大小 |
| 击中击不中算子 | **Hit and Miss operator** | 符号 A ⊛ B;basic shape detector;B 是要找的形状,A 是测试图;'patterns must match exactly' |
| 细化 | **thinning** | A ⊗ B = A − (A ⊛ B);用一组结构元 {B¹,...,Bⁿ} 反复应用直到图像不再变化;Matlab: bwmorph(img,'thin',Inf) |
| 加粗/增厚 | **thickening** | A ⊙ B = A ∪ (A ⊛ B);对背景做 thinning 再取反更快 |
| 骨架化 | **skeletonisation** | 英式 -isation(非 -ization);把物体缩为线条而不改变本质结构 |
| 求周长/周界提取 | **perimaterization (perimeter detection)** | 讲义拼作 'Perminatization'(疑笔误);确定二值图中物体的 perimeter pixels:像素为 on 且邻域中有 off 像素 |
| 灰度腐蚀/膨胀 | **greyscale erosion / dilation** | 英式 greyscale(非 grayscale);腐蚀算 A−B 取最小,膨胀算 A+B 取最大 |
| 结构元的域与值 | **domain and values** | 灰度形态学中 B 分两部分:domain b(哪些位置参与)与 values v;只对 domain=1 的像素加减值 |
| 补集/取反 | **complement / NOT (invert the image)** | Hit-and-Miss 中对 B 和 A 取补;Ā 表示 A 的补 |
| 互斥 | **mutually exclusive** | B ∩ B₂ = ∅;'sum(B AND B₂)=0';没有1处于相同位置 |
| 交集(逻辑与) | **intersection (logical AND)** | 取两结果的交集得到 hit-and-miss 结果 |
| 并集(逻辑或) | **union (logical OR)** | thickening 用 union |
| 噪声 | **noise** | 'the bane of image processing' |
| 加性噪声 | **additive noise** | Captured Image = Original Image + noise |
| 乘性噪声 | **multiplicative noise** | Captured Image = Original Image × noise;例:laser 的 speckle 散斑 |
| 椒盐噪声 | **salt and pepper noise** | 随机黑白点 random black and white spots;非线性滤波(中值)可去除 |
| 脉冲噪声 | **impulse noise** | 随机白色像素 random white pixels |
| 高斯噪声 | **Gaussian noise** | 强度的随机变化服从高斯/正态分布;很好地建模 sensor noise;线性平滑滤波擅长去除 |
| 信噪比 | **signal to noise ratio (SNR)** | 讲义举例 16dB;'lower SNR' 见 lighting 部分 |
| 卷积 | **convolution** | 符号 ⊗;h = f ⊗ g;邻域操作而非逐像素;denoise/edge detection/object detection |
| 卷积算子 | **convolution operator** | 噪声去除的实现方式 |
| 卷积掩膜/核 | **convolution mask / kernel** | g[i,k] 称为 mask 或 kernel;weighted kernel |
| 冲激响应 | **impulse response** | g(x,y) 为系统的 impulse response |
| 翻转核 | **flip the kernel (left/right and up/down)** | 因公式中负号索引需左右上下翻转 |
| 加权和 | **weighted sums (of image pixels)** | 卷积=图像像素的加权和 |
| 边缘问题 | **the edge problem** | 核中心落在边界时缺数据;解决:缩小输出 / 环绕 wrap around / 补零 surrounded by zeros / 复制边列 / 边界反射 reflected;quiz:'cope with the convolution edge problem' |
| 线性(性质) | **linear / convolution is linear** | 图像之和的卷积=各自卷积再相加 |
| 空间不变性 | **spatial invariance / spatially invariant** | 全图用相同滤波权重;反义 spatially varying filters |
| 互相关 | **cross-correlation** | 用于检测图中物体 detecting objects |
| 去噪 | **de-noising / denoise** | 讲义两种写法都有 |
| 均值滤波器 | **mean filter** | 核为邻域均值;3x3 即 (1/9)·全1矩阵;很好去高斯噪声;'mean has blurred the edges' |
| 中值滤波器 | **median filter** | 取邻域中值;保边 'median has persevered it';去椒盐噪声;不是卷积但有 sliding window 概念;答题:排序取中间值 |
| 高斯滤波器 | **Gaussian filter** | 权重为高斯函数形状;旋转对称 rotationally symmetric;σ 越大越平滑;单个中央波瓣 central lobe,权重从中心单调递减 |
| 低通滤波器 | **low pass filter** | mean/Gaussian 都是低通;丢失高频分量导致 blur |
| 高频分量 | **high frequency components** | 大核会损失高频,使图像模糊 |
| 线性平滑滤波器 | **linear smooth filters** | 擅长去高斯噪声 |
| 非线性滤波器 | **non-linear filters** | 可去椒盐噪声(如中值) |
| 模糊(图像) | **blur (an image)** | mean/Gaussian 的副作用;'blur sharp discontinuities','features become blurred' |
| 锐化(图像) | **sharpen an image** | 卷积常见用途之一 |
| 核大小 | **kernel size** | 典型 3x3, 5x5, 7x7;越大去噪越好但越模糊 |
| 权重和为1/归一化为单位 | **adds to unity** | 滤波核权重之和=1;大于1则整体变亮(等于信号处理的 gain 增益) |
| 对称核/非对称核 | **symmetric / non-symmetric kernel** | 非对称核在 x、y 方向产生不同结果 |
| 滑动窗口 | **sliding window** | 中值滤波虽非卷积但用相似的滑窗概念 |
| 阶跃/大阶跃函数 | **step / big step functions** | 中值滤波不影响大阶跃(不像均值滤波) |
| 尖峰 | **sharp peaks** | 中值滤波可能去掉 sharp peaks;200 值案例 |
| 低信噪比/大景深 | **lower SNR, increased DOF** | 亮光 + 小光圈(small aperture)→ 增大景深 depth of field;采光建议部分 |
| 分割 | **segmentation** | 'Get the lighting right! Segmentation becomes much easier' |
| 阈值/设阈值 | **threshold / thresholded result** | quiz:'Threshold set to 0.45';分割前先阈值化 |

## 卷积与边缘检测 Convolution & Edges

| 中文 | English | 备注 |
|---|---|---|
| 边缘检测 | **edge detection** | 本课主题；edge detector = 边缘检测算子 |
| 边缘 | **edge** | 定义:significant local changes in the image / 图像中显著的局部变化 |
| 图像分割 | **segmenting an image** | 讲义说 edge detection 是分割图像的 powerful method;名词 segmentation |
| 局部变化(显著的) | **significant local changes** | 边缘的定义短语 |
| 边界(两物体之间) | **boundary between two objects** | 边缘常出现处 |
| 不连续性 | **discontinuity** | 边缘与 intensity 或导数的 discontinuity 相关 |
| 图像强度 | **image intensity** | 美式无差异;intensity function = 强度函数 |
| 一阶/二阶导数 | **1st / 2nd derivative** | derivatives of image intensity;differentiate=求导 |
| 阶跃(边缘) | **step edge** | 四种边缘类型之一:an abrupt change in image intensity / 强度突变 |
| 斜坡(边缘) | **ramp** | a more gradual change / 较缓变化 |
| 线(边缘) | **line** | 近似为 step function 的微分 |
| 屋脊(边缘) | **roof** | low-pass approximation to a line / line 的低通近似 |
| 突变 | **abrupt change** | 形容 step edge |
| 微分/求导 | **differentiate / differentiation** | 动词 differentiate;名词 differential(讲义用 1st/2nd differential 指一/二阶微分图) |
| 阈值 | **threshold** | set a threshold;threshold selection problem=阈值选取问题;动词 thresholded |
| 绝对微分(大于阈值) | **absolute differential greater than threshold T** | 边缘判据短语 |
| 减去相邻像素 | **subtract the next door pixel** | 离散一阶差分的口语说法;next door pixel=相邻像素 |
| 二次微分/求导两次 | **differentiate twice** | 得到二阶导数 |
| 过零点 | **zero crossing (point)** | 高频考点!二阶导数过零=边缘;passes through zero indicates an edge |
| 峰值(给出边缘位置) | **peak (gives location of edges)** | 一阶导数在边缘处出现 local peak |
| 梯度算子 | **gradient operator** | 一阶微分在此语境的别名;first order differential |
| 梯度 | **gradient** | rate of change of the image intensity function / 强度变化率 |
| 变化率 | **rate of change** | gradient 是 a measure of the rate of change |
| 离散近似 | **discrete approximation** | discrete approximation to the gradient operator;images are discrete |
| 水平与垂直输出 | **horizontal and vertical outputs** | Gx, Gy;讲义:two outputs, vertical and horizontal |
| 幅值(梯度的) | **magnitude (of the gradient)** | 美/英拼写同;points in the direction of maximum rate of increase |
| 最大增长率方向 | **direction of the maximum rate of increase** | 梯度向量指向该方向 |
| 边缘角度/方向 | **angle / direction of the edge** | angle α from the x-axis;Laplacian 没有 edge direction measurement |
| 数值近似 | **numerical approximation** | derivatives approximated by differences / 用差分近似导数 |
| 差分 | **differences** | 2x2 first differences=2x2 一阶差分 |
| 卷积 | **convolution** | * 号;another major application of convolution;动词 convolve |
| 卷积掩模/卷积核 | **convolution mask / kernel** | mask 与 kernel 在本课通用;mask 偏小算子,kernel 偏 3x3 大核 |
| 滑动窗口 | **sliding window** | kernel that slides across the image=核在图上滑动;sliding window 比 FFT 慢(大核时) |
| 插值点 | **interpolated point** | 如 [i+½, j+½];interpolation needed=需插值 |
| 邻域 | **neighbourhood** | 英式拼写 neighbour-!2x2 neighbourhood;勿写 neighborhood |
| 中心像素 | **centre pixel** | 英式拼写 centre!3x3 mask 算 about a centre pixel;closer to centre of mask |
| Roberts 算子 | **Roberts** | 一阶;结果定位在 [i+½, j+½];continuous gradient 的近似 |
| Prewitt 算子 | **Prewitt operator** | 3x3 核;rows of 1 and -1 做 mean filter 去噪 + 边缘检测;quiz: Mask A 是 Prewitt 水平部分 |
| Sobel 算子 | **Sobel operator** | convolution mask, normally a=2;emphasis on pixels closer to centre |
| 均值滤波(去噪) | **mean filter / de-noise** | Prewitt 的行起 mean filter 作用;de-noise as well as edge detect |
| 二值图像 | **binary image** | 算子输出经 thresholded 得 binary image |
| 线性算子 | **linear operator** | 讲义强调 these are linear operators;反义 nonlinear rule |
| 一阶/二阶滤波器 | **first order / second order filter** | first order: Roberts, Sobel; second order: LoG, DoG |
| 拉普拉斯算子 | **Laplacian operator** | 二阶;∂²/∂x²+∂²/∂y²;只需一个核 no separate x and y,但无方向信息 |
| 差分方程 | **difference equations** | 二阶导数用 difference equations 近似 |
| 局部最大(梯度) | **local maxima in gradient values** | the better method:只取梯度局部最大点作为边缘 |
| 高斯滤波器 | **Gaussian Filter** | 用于 de-noise;Gaussian smoothing operator 会模糊边缘 |
| 高斯拉普拉斯 | **Laplacian of a Gaussian (LoG / LOG)** | 流程:Gaussian 去噪→Laplacian→zero crossing;两者都是 convolution kernel 可合并 |
| 亚像素精度 | **subpixel accuracy** | LoG 检测 zero crossing 给 subpixel accuracy(取决于噪声水平) |
| 带通滤波器 | **bandpass filter** | LoG 在 frequency domain 是 bandpass filter |
| 频域 / 空域 | **frequency domain / space domain** | DoG 图有 space domain 与 frequency domain 两种;Fourier 实现 |
| 高斯差分 | **difference of Gaussians (DoG / DOG)** | LoG 的近似;σ 比固定为 1.6;scale dependent=尺度相关 |
| 平滑/模糊(边缘) | **smoothing / blurring of edges** | Gaussian smoothing 导致 blurring;blurred edge=模糊边缘 |
| 参数 σ(标准差) | **parameter σ (sigma)** | large σ: better noise rejection but lose edge details |
| 噪声抑制/抗噪 | **noise rejection / robust against noise** | large σ better noise rejection;DoG is robust against noise;immune to noise |
| 尺度 | **scale** | edges of different scale;DoG filter is scale dependent;subpixel/different scales |
| 傅里叶变换 / 快速傅里叶变换 | **Fourier transform / FFT** | capital letters 表 Fourier transform;FFT 加速卷积(大核时更快) |
| 傅里叶域实现卷积 | **implement convolution using Fourier transforms** | 任何卷积都可用此法,大核时比 sliding window 快 |
| 非线性规则/滤波 | **nonlinear rule / non-linear mask** | quiz3:坏点用 median 或 mode filter 修正 |
| 中值滤波 / 众数滤波 | **median filter / mode filter** | quiz:replaced by the most common pixel value;mode filter not commonly used |
| 各向同性(算子) | **isotropic** | quiz:Mask B 是 second order isotropic differential operator(指 Laplacian) |
| 正脉冲 / 负脉冲 | **positive pulse / negative pulse** | quiz答题:Mask A 对正边产生 positive pulse;二阶:negative pulse 后接 positive pulse |
| 噪声 | **noise** | oscillations of small magnitude;disrupt the edge detector;二阶导数对 noise 更敏感 |

## 描述子:矩 / Hough变换 / 傅里叶描述子 (Descriptors: Moments / Hough Transform / Fourier Descriptors) — Week 6

| 中文 | English | 备注 |
|---|---|---|
| 描述子 / 形状描述子 | **descriptor / shape descriptor** | 课程核心词；shape descriptor = 对物体的低维描述，对3-D失真有一定不变性 |
| 特征向量 | **feature vector** | 讲义反复强调：descriptor的目的就是产生 a feature vector of low dimensionality |
| 降维 / 低维 | **low dimensionality / dimensionality reduction** | 英式无差异；讲义图标 'Dimensionality Reduction'，512x512=262,144 像素降到可管理量 |
| 模式识别 / 模式匹配 | **pattern recognition / pattern matching** | 标题 'Shape Descriptors and Pattern Recognition'；rapid 'matching' operation |
| 类内容差 / 类间区分 | **within-class tolerance / discrimination between classes** | 高频考点短语：within-class tolerance but discrimination between classes；也写 cluster classes together in feature space |
| 不变性 / (对…)不变 | **invariance / invariant to** | 如 invariant to scale, rotation, position；英式拼写无差异 |
| 二值掩膜 / 二值图像 | **binary mask / binary image / binary object** | B[i,j]，物体处为1否则为0；'Threshold them first – gives a binary mask' |
| 阈值化 | **threshold / thresholding** | 动词 threshold；'after threshold' |
| 矩 / 几何矩 | **moment / geometric properties (moments)** | 讲义标题 'Geometric Properties/Moments'；higher order moments |
| 零阶矩 | **zeroth moment** | = Area 面积；英式拼 zeroth |
| 面积 | **area** | Area = ΣΣ B[i,j]；'the simplest parameter is area'，也叫 zeroth moment |
| 一阶矩 | **first order moment(s)** | 用于求质心 centroid (x̄, ȳ) |
| 二阶矩 | **second order moment(s)** | 给出物体方向 orientation；'2nd order moment' |
| 质心 / 形心 | **centroid** | a.k.a. 'centre of area' / 'centre of mass'；务必英式 centre 不是 center |
| 质心 / 重心 (同义) | **centre of area / centre of mass** | 引号写法来自讲义；centre 英式拼写 |
| 方向 / 朝向 | **orientation** | 由二阶矩给出；'orientation of the object'；圆形 orientation is meaningless |
| 伸长轴 / 长轴方向 | **axis of elongation** | 'The axis of elongation is used to define the object' |
| 主轴 | **principal axis / principle axis** | 讲义原文误拼为 'principle axis'，标准应为 principal axis；考试写 principal 较稳 |
| 最小二阶矩的轴 | **axis of the least 2nd order moment** | Orientation = the angle of the axis of the least 2nd order moment |
| (物体的)伸长度 / 延伸度 | **elongation** | E = χmax / χmin；E=1 for a circle |
| 二阶面积矩 / 惯性矩 | **2nd moment of area (inertia)** | 'Object has smallest 2nd moment of area (inertia) about dashed line' |
| 最小二乘拟合 | **least square fit / least squares** | 拟合一条线使 χ² 最小 |
| (到直线的)垂直距离 | **perpendicular distance** | r = perpendicular distance from an object point to the line |
| 使…最小化 | **minimise** | 英式 -ise；'find the line that minimises…'；the sum of squared distances is minimised |
| 平方距离之和 | **sum of the squared distances** | 二阶矩轴定义：sum of the squared distances between the object points and the line is minimised |
| 直线的极坐标方程 | **polar co-ordinate equation of a line** | ρ = x cosθ + y sinθ；用来避免梯度无穷大 (avoid infinities in the gradient)；英式 co-ordinate 带连字符 |
| 最大方差方向 / 主方向 | **direction of max variance** | 图中 e1 = direction of max variance, e2 垂直方向 |
| 面积 / 长轴 / 短轴长度 | **area / long axis length / short axis length** | Possible metrics 列表；也有 average radius, position |
| 高阶矩 | **higher order moments** | 'We can do higher and higher moments, but the maths gets even more complex' |
| 形状因子 / 形状因数 | **form factor / form-factor** | Formfactor = 4π·area / perimeter²；常考公式量 |
| 圆度 | **roundness** | Roundness = 4·area / (π·MaxDiameter²) |
| 密实度 / 固性 | **solidity** | 讲义拼 'Solidy'(误)，标准 Solidity = Area / ConvexArea |
| 长宽比 / 纵横比 | **aspect ratio** | AspectRatio = MaxDiameter / MinDiameter；= length/width，independent of scale and in-plane rotation |
| (基于纤维的)伸长度 | **elongation (= FibreLength / FibreWidth)** | common metrics 中；注意英式 Fibre 不是 Fiber |
| 范围比 / 占空比 | **extent** | Extent = NetArea / BoundingRectangle (讲义拼 Rectange，应为 Rectangle) |
| 凸度 | **convexity** | Convexity = ConvexPerimeter / Perimeter |
| 卷曲度 | **curl** | Curl = Length / FibreLength；英式 Fibre |
| 紧致度 | **compactness** | 出现在 shape 参数对比表 (Roundness/Convexity/Solidity/Compactness) |
| 凸面积 / 凸周长 / 凸包 | **convex area / convex perimeter** | 用于 solidity 和 convexity 公式 |
| 周长 | **perimeter** | form factor、convexity 公式中的量 |
| 外接矩形 | **bounding rectangle** | extent 公式中；讲义拼 Rectange (误) |
| 无量纲形状参数 | **dimensionless shape parameter(s)** | 'dimensions cancel'；'unlimited number of different shapes can have identical values' |
| Hough变换 | **Hough transform** | 用于 finding line parameters；voting process |
| 参数空间 | **parameter space / ab space / ρθ space** | xy space 中一点 = ab space 中一条线 (a single point in xy yields a line in ab) |
| 梯度 / 斜率 | **gradient / slope** | line y = ax + b，a is the gradient (slope) |
| 截距 / y轴截距 | **intercept / y axis crossing point** | b is the intercept；竖直线时 a = infinity 是问题所在 |
| 参数形式 / 函数形式 | **parametric form / functional form** | line in parametric form y = ax + b |
| 正弦曲线 / 正弦 | **sinusoidal curve / sinusoid** | ρ = x cosθ + y sinθ 产生 a set of sinusoidal curves in ρθ space；each point creates a sinusoid |
| 投票过程 / 投票 | **voting process / single vote** | 考点：'It is a voting process'；each point contributes a 'single vote' for a line passing through it |
| (Hough空间中的)峰 / 峰值检测 | **peak (in Hough space) / peak detector** | a peak in Hough space corresponds to a line；application of a peak detector yields ρ,φ parameters |
| 对噪声不敏感 / 鲁棒 | **noise insensitive / robust to noise and errors** | 因峰由多点构成，缺失段仍可检出 (still detectable if the line has missing segments) |
| 广义Hough变换 | **generalised Hough (transform)** | 英式 generalised；'generalisation of the Hough transform to other shapes' |
| 圆的方程 / 圆检测 | **equation of a circle / Hough circle detection** | (x−a)² + (y−b)² − r² = 0；需 3D parameter space (unknown radius and position) |
| 定位 / 局部化 | **localise / localisation** | 英式 -ise/-isation；'detect and localise circles'；'subpixel localisation' |
| 在通道/边缘点上累加 (投票累加) | **they then add up / accumulate** | sinusoids overlap in the presence of a straight line, then add up |
| 膨胀 / 膨胀版边缘图 | **dilated version (of the detected edges)** | Hough检测线需用 dilated/filtered edge map 做掩膜 (masked by a dilated version) |
| 毛刺 / 去毛刺 | **spurs / pixel erosion** | 'spurs are removed with a pixel erosion operation'；erosion 收缩前景 |
| 腐蚀 (收缩前景) | **erosion (shrinks the foreground)** | 答题常用表述；erosion 使前景收缩 |
| 傅里叶描述子 | **Fourier descriptor(s)** | 用边界点序列做 DFT；可丢弃高阶项 |
| 傅里叶系数 | **Fourier coefficient(s)** | Possible Algorithms 中列出；M coefficients 作为 low dimensional descriptor |
| 傅里叶级数 | **Fourier series** | 方波由 set of cosine terms 构成 (made from set of cosine terms) |
| (数字)边界 / N点边界 | **(digital) boundary / N-point digital boundary** | 起始于任意点 (x0,y0)，绕边界移动得坐标对 |
| 坐标对 | **coordinate pairs** | (x0,y0),(x1,y1)…(xN,yN)；s(k) = [x(k) y(k)] |
| 复数 | **complex number** | 每个坐标对视为复数 s(k) = x(k) + j y(k) |
| 实轴 / 虚轴 | **real axis / imaginary axis** | x-axis 是 real axis，y-axis 是 imaginary axis |
| 离散傅里叶变换 | **discrete Fourier transform (DFT)** | 对 s(k) 取 DFT；'a 2-D problem is reduced to a 1-D feature vector' |
| 低通滤波 | **low pass filtering / low pass filter** | 丢弃高阶项 = low pass filtering the data = blurring by convolution |
| 卷积 / 模糊 | **convolution / blurring** | 等价于 blurring the data by convolution；smoothes the shape's outline |
| 低频项 / 高频项 | **low frequency terms / high frequency terms** | 低频=粗略形状与大小 (rough shape and size)，高频=细节 (fine detail) |
| 高阶项 / 丢弃高阶项 | **higher order terms / dropping higher order terms** | 'start dropping some of the higher order terms'，使描述子对噪声/误差更鲁棒 |
| 展开边界 (得到1D曲线) | **unrolling the boundary (to get 1D graph)** | ρ–φ 表示：r(θ) 圆为常数线，方形为周期尖峰 (√2A 峰值) |
| 重建 / 重构 | **reconstruction** | 'reconstructions from Fourier descriptors for various values of M' |
| 正方形与圆给出不同输出 | **square and circle give different outputs** | r(θ): circle = flat line at A; square = peaks up to √2A — 经典考图对比 |
| 可能的算法 / 分类方法 | **possible algorithms** | Shape descriptors / Fourier coefficients / Texture / Correlation / Model fitting / Feature matching (SIFT) |
| 纹理 / 相关 / 模型拟合 | **texture / correlation / model fitting** | 其它分类法；correlation, SIFT, texture analysis, colour histograms 可能 'over kill' |
| 颜色直方图 | **colour histograms** | 务必英式 colour 不是 color |
| 特征匹配 (SIFT) | **feature matching (SIFT)** | Possible Algorithms 列表项 |
| 分类 / 分类器 | **classification / classify** | 'detect and classify our object'；classification is prone to error |
| (在)x轴间的夹角 | **angle between … and the x-axis** | quiz原题：calculate θ, the angle between the object's least second moment of area and the x-axis |
| 亚像素定位 / 亚像素 | **subpixel localisation / subpixel** | 英式 localisation；上一节 'Subpixel Location Estimation' |
| 边界段 (开/闭曲线) | **boundary segment (open curve) / object boundary (closed curve)** | open curve = 边界段；closed curve = 完整物体边界 |
| 标注 / 区域标注 | **labelling / region labelling** | 务必双 l：labelling (英式)；流程图 'Region Labelling' |
| 邻域 / 邻接 | **neighbourhood** | 英式 neighbour；edge following uses neighbourhood to fill in gaps |
| 直方图均衡化 | **histogram equalisation** | 英式 -isation；图像增强流程中出现 |
| 对所有通道/点求和 (累加求和) | **sum across (all points) / ΣΣ over i,j** | 矩公式 Area = Σ_{i}Σ_{j} B[i,j]；答题写 'sum over all pixels / sum across' |

## 分类与深度学习:CNN/池化/BatchNorm (Classification & Deep Learning)

| 中文 | English | 备注 |
|---|---|---|
| 分类 | **classification** | 动词 classify (classify our object) |
| 模式 | **pattern** | a pattern is an arrangement of descriptors |
| 模式类别 | **pattern class** | a family of patterns that have something in common |
| 形状描述子 | **shape descriptors** | 讲义写 'shaped descriptors'，eg area, position, moments |
| 特征向量 | **feature vector** | Pattern Matching of Feature Vector；'feature matching (SIFT)' |
| 特征提取 | **feature extraction** | 流程图框名 |
| 降维 | **dimensionality reduction** | 流程图侧边标注，注意 -ity 拼写 |
| 量化描述子 | **quantitative descriptors** | Vectors used for quantitative descriptors |
| 判别(决策)函数 | **discriminant (or decision) function** | 标题原文，dᵢ(x)>dⱼ(x) 时归类 i |
| 决策边界 | **decision boundary** | 考试高频；'a line'，dᵢ(x)−dⱼ(x)=0；Quiz8 要求 find a decision boundary equation |
| 类别均值 | **the mean of each class** | mⱼ=(1/Nⱼ)Σxⱼ；'Locate the mean of each class'；'Find the means' |
| 模式向量个数 | **number of pattern vectors** | Nⱼ is the number of pattern vectors in each class |
| 欧氏距离 | **Euclidean Distance** | Dⱼ(x)=‖x−mⱼ‖；最小化此距离 |
| 最小距离分类器 | **minimum distance classifier** | 讲义思路：minimum of Dⱼ = maximum of (xᵀmⱼ−½mⱼᵀmⱼ) |
| 转置 | **transpose** | xᵀx 记作 'x transpose x' |
| 曼哈顿/城区距离 | **City block (distance)** | 其它距离度量之一 |
| 马氏距离 | **Mahalanobis distance** | other metric |
| 汉明距离 | **Hamming distance** | other metric |
| 训练数据 | **training data** | split into training data and validation data |
| 验证数据 | **validation data** | vs training data |
| 未见过的数据 | **unseen data** | Test your classifier on different unseen data! |
| 训练分类器 | **train your classifier** | answer 常用动词短语 |
| 最近邻分类器 | **Nearest Neighbour Classifier** | 英式 neighbour，勿写 neighbor |
| k近邻 | **kNN – k nearest neighbour** | each class votes to give the final result |
| 决策树/树 | **tree / decision tree** | use trees for classifying our data |
| 叶节点 | **leaf nodes** | the nodes that produce an answer are known as leaf nodes |
| 二元划分 | **binary split** | test all possible binary splits |
| (节点)纯度 | **purity / pure** | stop when the node is pure；looks for purity in the node |
| 误分类比例 | **fraction of misclassified classes** | optimisation parameter 衡量 |
| 最小叶参数 | **MinLeaf parameter** | 该课特有写法 MinLeaf / minLeaf；child node too few observations |
| 停止规则 | **stopping rule** | 控制树深 |
| 树深度 | **tree depth** | deep tree 易 over train；shallow trees are more robust |
| 过拟合/过训练 | **over train / over fit** | 讲义两种写法都出现 (over train, over fit) |
| 交叉验证 | **cross validating / cross validate** | split training data into 10 parts；measures predictive accuracy |
| 剪枝 | **pruning** | merging leaves on the same tree branch；reduced error pruning |
| 优化准则/参数 | **optimisation criterion / optimisation parameter** | 讲义此处用英式 -sation；但同页也出现 optimization(美式)，答题统一写 optimisation |
| 随机森林 | **random forests** | multiple trees trained on random subsets of data；each tree votes |
| 支持向量机 | **Support Vector Machines (SVM)** | works with 2 classes only |
| 超平面 | **hyper-plane** | 讲义带连字符 hyper-plane；best = largest margin |
| 间隔 | **margin** | the largest margin between the two classes |
| 支持向量 | **support vectors** | data points closest to the separating hyper-plane |
| 径向基(高斯)函数 | **Radial functions (Gaussian)** | 非线性核之一；另有 Polynomials, Tan sigmoid |
| 神经网络 | **Neural Networks** | crude approximation to a biological neuron |
| 感知机 | **Perceptron** | feedforward neural network；computer vision 重要类型 |
| 前馈神经网络 | **feedforward neural network** | Multilayer feedforward neural network model |
| 权重 | **weight(s)** | each input is multiplied by some weight, summed, a bias added |
| 偏置 | **bias** | a bias added；Quiz7 'Assume that the bias is zero' |
| 激活函数/激活元 | **activation function / activation element** | non-linear function applied |
| Sigmoid函数 | **sigmoid function** | y=1/(1+exp(−x))；squashes input to 0→1；大网络收敛差 |
| 双曲正切 | **tanh function** | only common in small networks |
| ReLU(修正线性单元) | **ReLU (Rectified Linear Unit)** | y=max(0,x)；Quiz7 'What is the output if a ReLU activation is applied?'；sets all values below 0 to 0 |
| 权重初始化 | **Weight Initialisation** | 英式 -isation；all zero / Gaussian Random / Xavier |
| Xavier初始化 | **Xavier** | variance related to number of input neurons |
| 训练 | **training** | run a gradient descent algorithm over all training images |
| 梯度下降 | **gradient descent** | used to find the weights |
| 全连接网络 | **fully connected neural network** | all neurons fully connected to every other layer |
| 反向传播 | **backpropagation** | calculates which direction to move the weights (error gradient) |
| 误差梯度 | **error gradient** | gets smaller further from the final layer |
| 梯度消失问题 | **vanishing gradient problem** | 考点；error gradient lost with machine precision → 训练 stagnation |
| (训练)停滞/饱和 | **stagnation / saturate** | network stagnates；sigmoid 可 saturate (becomes about value 1) |
| 平移不变性 | **shift invariant / shift invariance** | output of a NN is not shift invariant；max pooling gives some shift invariance |
| 尺度/旋转不变 | **scale, or rotation invariant** | NN 扫描法 not scale or rotation invariant |
| 深度学习 | **Deep Learning** | neural network with a large number of layers |
| 卷积神经网络 | **Convolutional neural networks (CNN)** | 标题；change from sigmoid to ReLU for hidden layers |
| 权重共享 | **Weight sharing** | repeat the weight rather than having them all different；massively reduces number of weights |
| 卷积算子/卷积 | **convolution operator / convolution** | CNN 'identical to the convolution operator'，实则 cross-correlation |
| 互相关 | **cross-correlation** | 'actually it is the related cross-correlation'；scanning network ≈ correlation/convolution |
| 输入通道(RGB) | **input channel (RGB)** | Quiz7/8 'with 3 channels'；the 3 is the number of channels |
| 跨通道求和 | **sum (across channels)** | 3D kernel: 'simply multiply, and sum as you would for the 2D case' |
| 逐像素相乘 | **pixel-wise multiplication** | Quiz7 solution: 'perform a pixel-wise multiplication of each element with the filter kernel' |
| 卷积核/滤波核 | **kernel / filter kernel** | Conv2D kernel size 3×3×3；3D kernel；'5x5x3 + 1 bias = 76 weights' |
| Conv2D层 | **Conv2D layer** | Quiz 用语：output activations of a Conv2D layer |
| 输出激活/激活图 | **output activations / activation map** | '10 activation map stacked'；output of a Conv2D layer |
| 隐藏层 | **hidden layer(s)** | ReLU used for the hidden layers |
| 边缘滤波器 | **edge filters** | first layers act like edge filters；higher order structure in higher layers |
| 池化层 | **Pooling Layer** | used to reduce the size of the image |
| 最大池化 | **MaxPooling / max pooling** | takes the max output value；Quiz8 计算 MaxPooling，stride 2×2 |
| 平均池化 | **AveragePooling / Average Pooling** | Quiz8 计算 Average Pooling |
| 全局平均池化 | **GlobalAveragePooling** | Quiz8 输出单一值 (eg 1.19) |
| 步幅 | **stride** | Quiz8 'Assume a stride of 2×2 for the first two' |
| 下采样 | **down sampling** | max pooling → down sampling；letting through the strongest signal |
| 丢弃法 | **drop out** | 'Other new layers such as Max-pooling, drop out' |
| 批归一化 | **batch normalisation** | 务必英式 -isation；Quiz8 'Batch normalisation is performed on this data' |
| 批大小 | **batch size** | Quiz8 'a batch size of 2' |
| 批均值 | **batch mean** | Quiz8 'The mean image of the batch is (Batch mean)' |
| 批方差 | **batch variance** | Quiz8 solution 'Batch variance' |
| 缩放/平移参数γ,β | **gamma (γ) and beta (β)** | BatchNorm 学到的参数；ε=1×10⁻⁶ (epsilon) |
| 数据归一化 | **normalise / normalised** | 英式拼写；'Data is not normalised when using images'，注意勿写 normalize |
| 数据增强 | **Data augmentation** | rotate, skew the training data and add to training set |
| 激活图堆叠 | **stacked (activation maps)** | 32×32×3 with 10 5×5 filters → 28×28×10 |
| VGG网络 | **VGG-19 network** | a common design：blocks of Conv2D + max pooling，then fully connected + softmax |
| Softmax | **softmax** | VGG 输出层；fully connected+ReLU → softmax |
| 目标检测 | **Object Detection** | find objects in an image (vs classify whole image) |
| 区域候选 | **region proposals** | R-CNN: extract region proposals (~2k)；warped region |
| R-CNN | **R-CNN** | (2014) works well but its slow |
| YOLO | **YOLO (You Only Look Once)** | single network that takes the whole image；bounding boxes + confidence |
| 边界框 | **bounding boxes** | YOLO：bounding boxes + confidence；class probability map |

## 互相关 / SIFT / 图像配准RANSAC / Viola-Jones 术语对照表（Week 8 + Week 9，英式拼写）

| 中文 | English | 备注 |
|---|---|---|
| 互相关 / 交叉相关 | **cross correlation** | 讲义标题 'Correlation (a.k.a. cross correlation)'；也单写 correlation。是一种模板匹配技术 |
| 模板匹配 | **template matching** | 2D template matching；Viola-Jones 那节标题也是 'Template matching' |
| 模板 / 目标模式 | **template / target pattern** | 'A target pattern is shifted to every location'；模板图叫 template image |
| (加权)核 | **(weighted) kernel** | 'A weighted kernel is moved across the image'；kernel 就是要找的物体 |
| 卷积 | **convolution** | correlation 与 convolution 类似；注意 correlation ≠ convolution，但都用核滑过图像 |
| 卷积算子 / 卷积运算 | **convolution operator** | 'Note the similarity with the convolution operator' |
| 归一化 / 标准化 | **normalisation** | 英式 -isation 不是 -ization；归一化项=the normalisation term；归一化形式=normalised form |
| 归一化互相关 | **normalised cross correlation** | 图注 'Normalised Cross correlation'；克服局部亮度变化影响 |
| 归一化项 / 分母 | **normalisation term (the denominator)** | 计算慢，常省略；省略后会受图像亮度影响 |
| 减去均值 / 零均值模板 | **subtract the mean / zero-meaned mask** | 答题短语：'subtract the mean from the input and template images first'；图注 'zero-meaned mask'，更省算力的替代方案 |
| (相关)峰值 | **(correlation) peak** | 'It produces a peak where they match'；correlation peak / correlation-peak result |
| 峰值检测 | **peak detection** | 图注 'Peak detection result' |
| 真目标 / 假目标 | **true target / false target** | Fig.3/Fig.4 'Correlation peak result for the true/false target' |
| 平移不变 | **shift-invariant** | 答题点：correlation 'is shift-invariant – the peak moves with the target'；对 rotation/scale 敏感，但对位置(平移)不敏感 |
| 对…敏感 | **sensitive to (rotation, scale)** | 'Its sensitive to Rotation, Scale' |
| 杂乱场景 | **cluttered scene** | 'picking out a shape from a cluttered scene'；Matlab 例 cluttered2d.m |
| 傅里叶域 / 傅里叶变换 | **Fourier Domain / Fourier Transforms** | 相关可在频域做以降低计算量；公式 F{f⊗h}=F(u,v)H*(−u,−v) |
| 复共轭 | **complex conjugate** | 'Using the complex conjugate image is the simplest version' |
| 匹配滤波器 | **matched filter** | 用复共轭图像即 matched filter；缺点：易与不想要的物体高相关 |
| 逆滤波器 / 维纳滤波器 | **inverse filter / Weiner filter** | 讲义拼作 'Weiner'(实为 Wiener)；改进相关的滤波方法 |
| DOG滤波 / 高斯差分 | **DOG filter (Difference of Gaussians)** | 'DOG filter the image'；找边缘、去噪、不让零频通过(等价于减均值) |
| 零频 | **zeroth frequency** | 'does not allow the zeroth frequency through – this is the same as subtracting the mean' |
| 去噪 | **de-noise / denoise** | 讲义两种写法都出现；de-noising kernel / denoise the image |
| 线性运算 | **linear operation** | 'Correlation is a linear operation' → 可把多个相关滤波器相加 |
| 多滤波器组合 / 加权相加 | **adding filters together (with weights)** | 应对旋转：H_total = a1·H1 + a2·H2 + a3·H3，权重选择是研究难点 |
| 信噪比 | **signal to noise ratio (SNR, dB)** | 图轴 'SIGNAL TO NOISE RATIO (dB)'；1/12/72 个训练滤波器对比 |
| 面内旋转 | **in-plane rotation** | 图轴 'IN-PLANE ROTATION (degrees)'；'not just in plane rotations'，理想要 3D 旋转不变 |
| 仿射变换 | **affine transform / affine transformation** | correlation 不是 affine transform invariant；由相机视角改变引起 |
| 投影变换 | **projective transform** | Week9 'affine and projective transform'；铁轨图示 Projective |
| 尺度/旋转/仿射不变 | **scale / rotation / affine invariant** | 答题对比：correlation is NOT scale/rotation/affine invariant；SIFT 则可应对 |
| Haar类特征 | **Haar like features** | Viola-Jones 核心；二值特征(0或1)；分 Edge/Line/Four-rectangle features |
| 边缘特征 / 线特征 / 四矩形特征 | **Edge Features / Line Features / Four-rectangle features** | Haar 特征三类(a)(b)(c) |
| 白区像素和减黑区像素和 | **sum of pixels under white minus sum under the black** | Haar 特征值的算法，关键答题短语 |
| 分类器 | **classifier** | Haar 特征送入分类器(Ada-boost, neural network 等)判断是否目标 |
| 弱分类器 / 强分类器 | **weak classifier / strong classifier** | 每个特征=weak classifier(优于50%)；'Combining lots of weak classifiers gives a strong classifier' |
| AdaBoost | **Ada-boost** | 讲义写作 'Ada-boost'/'ada-boost'；输出 H(x)=sign(Σ αt·ht(x))，ht 输出 1 或 -1 |
| 积分图 / 求和面积表 | **Integral Images (Summed area table)** | 加速技巧；每点=其上方与左方像素和加原图 i(x,y) |
| 积分图求和 | **Integral image sum = A + D − B − C** | 四次运算即可算 Haar 特征，'very fast' |
| 级联 / 分组拒绝 | **test weak classifiers in groups; reject and move on** | 加速：第一个失败就拒绝跳过(级联思想) |
| 人脸检测器 | **Haar face detector** | 24x24 窗 → 160,000 特征；约 6000 特征分类一张脸 |
| 尺度不变特征变换 | **Scale-Invariant Feature Transform (SIFT)** | 匹配显著特征而非整个物体 |
| 关键点 | **keypoints** | 加粗术语；'significant features are matched instead - keypoints'；也叫 control point / keypoint matching |
| 关键点选取 / 关键点定位 | **Keypoint Selection / Keypoint locations** | 函数输入图像返回关键点列表；用 DOG 在多尺度上定位强边缘 |
| 尺度参数 sigma | **scale parameter, sigma** | DOG 依赖尺度参数 σ；'repeat the process at several scales' |
| 多尺度 | **several scales / multiple scales** | 只保留在多个尺度上都出现的特征 → 稳定关键点 |
| 稳定关键点 | **stable keypoints** | 'This gives very stable keypoints' |
| 低对比度点(阈值剔除) | **low contrast candidate points (by thresholding)** | 低对比度候选点与沿边缘的区域被剔除 |
| 局部描述子 | **Local Descriptors** | 每个关键点得到一个描述其周围图像的数字向量 |
| 边缘梯度方向 / 主方向 | **edge gradient directions / dominant direction** | 计算梯度方向并找主方向(dominant direction / dominant edge) |
| 重定向到0度(旋转不变) | **reoriented so the dominant edge is at 0 degrees** | 关键答题点：使特征向量 rotational invariant |
| 128维直方图/特征向量 | **128 element histogram / feature vector** | 边缘的 128 元素直方图即 feature vector |
| 特征匹配做物体识别 | **object recognition by matching features** | 标题；一类图像的特征集构成该类的 signature(签名) |
| 欧氏距离最近匹配 | **nearest match using Euclidean distance** | 在训练特征中找最近匹配 |
| 最佳匹配优于次佳匹配 | **best match much better than the second-best match** | 只保留满足此条件的匹配(Lowe 比率检验思想) |
| 词袋方法 | **bag-of-features approach** | 仅用匹配集合分类图像；几何关系可进一步精化 |
| 广义霍夫(变换) | **generalised Hough** | 英式 generalised；用特征间几何关系做物体分类 |
| 图像配准 | **Image Registration** | 把同一场景多张图合并到一个坐标系；用途：合成照片、医学图像、地图 |
| 参考图/固定图/基准图 | **reference (fixed or base) image** | 指定一张为参考，其余对齐到它 |
| 空间变换矩阵 | **spatial transformation matrix** | apply spatial transformation matrix so they align with the reference |
| 平移/缩放/错切/旋转 | **Translation / Scale / Shear / Rotation** | Affine Transform 四种基本变换及其变换矩阵；shear=错切(shx/shy) |
| 控制点 | **control points** | Control Point Method；两图选对应控制点算变换矩阵；与 SIFT 特征匹配相同 |
| 控制点手动/自动选取 | **Manual / Automatic Selection of Control Points** | 自动选取即用 SIFT 特征匹配 |
| 迭代匹配 | **Iterative matching** | 流程：Image Transform Matrix → Bilinear Interpolation → Metric → Stop? → Optimise |
| 双线性插值 | **Bilinear Interpolation** | 迭代匹配流程中的插值步骤；配准涉及图像 interpolation |
| 度量(均方误差) | **Metric: mean square error** | 度量=两图差异(MSE) |
| 优化器(梯度下降) | **Optimiser: gradient descent** | 'Optimiser: gradient descent, etc'；优化变换矩阵参数(旋转/尺度/平移) |
| RANSAC(随机抽样一致) | **RANSAC: RANdom SAmple Consensus** | 减少 SIFT 错误匹配；随机选子集→测拟合→重复(如100次)→取最佳拟合集 |
| 单应矩阵 / 投影矩阵 | **homography matrix / projection matrix** | x́ = H x；3x3 投影矩阵 H，由两组匹配点经矩阵求逆求得 |
| 内点 / 外点(错误匹配) | **inlier / outliers (false matches)** | 图注 '580 (92.65%) inlier matches out of 626'；RANSAC 去除 false matches(outliers) |
| 试探性匹配 | **tentative matches** | 图注 '626 tentative matches' |
| 阈值 t | **threshold, t** | 应用 H 后，匹配点应在阈值 t 内对齐；数 below t 的匹配数 |
| 图像拼接 / 镶嵌图 | **Image Stitching / Mosaic / panorama stitching** | 图注 'Mosaic'；SIFT 应用之一 panorama stitching |
| 基准标记 | **fiducial markers** | SIFT 需自然图像(大量独特关键点)；否则可用 fiducial markers(如圆形) |
| 目标计数与标注 | **Object Counting and Labelling** | Week9 章节；注意 Labelling 双 l(英式) |
| 标注 / 打标签 | **Labelling** | 给每个独立物体唯一标识码；双 l 拼写 |
| 二值阈值分割 | **Binary thresholding** | 最简单方法；'Iterative threshold selection method'(迭代阈值选择) |
| 形态学算子(腐蚀/膨胀) | **morphological operators (erosion / dilation)** | 用 eroding then dilating 去小噪点；erosion 估计背景去光照变化 |
| 前景收缩 | **shrinks the foreground** | erosion 的效果描述(常考答题短语，对应腐蚀使前景缩小) |
| 连通区域(4/8连通) | **connected regions (4-connected / 8-connected)** | 4连通=边相邻；8连通=边或角相邻(含对角) |
| 连通成分分析 | **connected component analysis** | 扫描到值为1的像素时执行；用 equivalence table 处理等价标签 |
| 等价表 / 等价标签 | **equivalence table / equivalent labels** | 多个邻居有标签时记为等价，存入等价表 |
| 邻居 / 邻域 | **neighbours** | 英式 neighbour(含 u)；'if all four neighbours are zero, assign a new label' |
| 伪彩色图 | **False colour map** | 英式 colour；'False colour map showing labelled objects' |
| 质心(中心) / 面积 / 高阶矩 | **centre of mass / area / higher order moments** | 英式 centre；可对每个 grain 测量位置/面积/高阶矩 |
| 对比度拉伸 | **stretch the image contrast** | 公式 g = (I−I1)/(I2−I1)·(Imax−Imin)+Imin |
| 灰度 / 灰度级 | **grey level / grey level intensity** | 英式 grey 不是 gray；'complex grey level images' |
| 地平面估计 | **Ground Plane Estimation** | 假设地面平坦、相机固定高度；用投影矩阵从图像坐标映射到真实世界 |
| 颜色分割 | **Colour segmentation** | 英式 Colour；找球/锥体先做颜色分割再形态学清理 |
| 深度学习分割 / 语义分割 | **Deep Learning Segmentation** | 识别实际物体来分割；用自编码器(auto-encoder)结构如 SegNet/VGG19 CNN |
